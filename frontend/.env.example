NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
# Espejo UX del flag del backend (el backend es la fuente de verdad)
NEXT_PUBLIC_INE_REQUIRED=false
# Dominio público de este frontend — usado para armar la URL que codifica
# el QR de validación de recibos (reciboHelpers.ts:buildValidacionUrl).
# Si no está definida, cae al dominio de producción en Railway.
NEXT_PUBLIC_APP_URL=http://localhost:3000
